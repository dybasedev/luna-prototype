# Luna Prototype Showcase 模块

提供描述前端页面的组件，生成前端页面描述结构体，前端通过适配可以根据后端输出生产对应的前端页面，
可以实现常用功能组件、页面的热更新。也可以通过此模块实现更多意想不到的功能。

## 项目结构

- `Structures`，描述前端页面结构或者封装组件属性的组件，这类描述对象用于前端根据其生成符合结构的组件
- `Adapters`，适配器，将后端数据转换成前端页面结构对象，可以采用不同的适配器转换针对不同前端架构的产物